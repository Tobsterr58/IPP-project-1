<?php
    ini_set('display_errors', 'stderr');

    if ($argc > 1) {
        if ($argv[2] == "--help") {
            echo "Usage: php parser.php [file] [options]
            exit(0);
        }
    }
        
?>